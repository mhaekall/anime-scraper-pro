# Shared Resources
This directory contains code and definitions shared between `apps/` and `services/`.

- `schemas/`: Database schema definitions (SQL/Drizzle).
- `types/`: Common TypeScript and Python data models.
- `utils/`: Universal utility functions.
